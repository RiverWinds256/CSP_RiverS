// River Stanley string notes C


#include <stdio.h>
#include <strings.h>

char subject[50];

char name[] = "Victoria";

char sentence[];

int main(void){
    printf("what class are you in? ");
    //scanf("%s", subject);
   // fgets(subject, sizeof(subject), stdin);
    //printf("you are in %s, that is a cool class", subject);
//name[0] = 't';
//name[1] = 'o';
//name[2] = 'r';
//name[3] = 'i';

char one [] = "hello";

char two[] = "world!";

strcat(one, two);
//    printf("%lu",sizeof(sentence));
//printf("%d/n",strlen(sentence));
return 0;
}

